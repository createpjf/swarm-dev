# ── Core (required) ──
httpx>=0.24.0
pyyaml>=6.0
filelock>=3.10.0
rich>=13.0.0
questionary>=2.0.0

# ── Memory (recommended) ──
chromadb>=0.4.0

# ── On-chain (optional) ──
# web3>=6.0.0
# eth-account>=0.9.0
